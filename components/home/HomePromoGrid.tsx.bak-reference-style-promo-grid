import Link from "next/link";

const smallCards = [
  {
    label: "Inside Story",
    title: "Inside Story",
    image: "/images/categories/skincare.jpg",
    href: "/shop?category=skincare",
  },
  {
    label: "Hot Deals",
    title: "Beat the Heat",
    image: "/images/categories/haircare.jpg",
    href: "/shop",
  },
];

export default function HomePromoGrid() {
  return (
    <section className="bg-[#fcf8f6] px-3 py-4 md:px-6 lg:px-8">
      <div className="mx-auto max-w-7xl space-y-4">
        <Link
          href="/shop"
          className="group relative block h-[170px] overflow-hidden rounded-[20px] border border-[#ead9d1] bg-[#2e221d] sm:h-[220px] md:h-[260px]"
        >
          <img
            src="/images/categories/perfume.jpg"
            alt="Top Picks"
            className="h-full w-full object-cover transition duration-500 group-hover:scale-105"
          />

          <div className="absolute inset-0 bg-gradient-to-r from-black/70 via-black/25 to-transparent" />

          <div className="absolute left-5 top-1/2 -translate-y-1/2 text-white sm:left-8">
            <p className="text-[10px] font-semibold uppercase tracking-[0.28em] text-white/80 sm:text-xs">
              Top Picks
            </p>
            <h2 className="mt-2 text-3xl font-bold sm:text-5xl">Top Picks</h2>
            <p className="mt-2 text-xs font-semibold text-white/90 sm:text-sm">
              Explore now
            </p>
          </div>
        </Link>

        <div className="grid grid-cols-2 gap-4">
          {smallCards.map((card) => (
            <Link
              key={card.title}
              href={card.href}
              className="group relative h-[155px] overflow-hidden rounded-[18px] border border-[#ead9d1] bg-[#2e221d] sm:h-[220px] md:h-[260px]"
            >
              <img
                src={card.image}
                alt={card.title}
                className="h-full w-full object-cover transition duration-500 group-hover:scale-105"
              />

              <div className="absolute inset-0 bg-gradient-to-t from-black/65 via-black/20 to-transparent" />

              <div className="absolute inset-x-3 bottom-4 text-center text-white sm:inset-x-5">
                <p className="text-[9px] font-semibold uppercase tracking-[0.22em] text-white/75 sm:text-xs">
                  {card.label}
                </p>

                <h3 className="mt-1 text-xl font-bold leading-tight sm:text-3xl">
                  {card.title}
                </h3>

                <p className="mt-2 text-[10px] font-semibold text-white/85 sm:text-xs">
                  Read more
                </p>
              </div>
            </Link>
          ))}
        </div>
      </div>
    </section>
  );
}